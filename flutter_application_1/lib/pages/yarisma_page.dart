import 'package:flutter/material.dart';

class YarismaPage extends StatelessWidget {
  const YarismaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Yarışma Sayfası')),
    );
  }
}
